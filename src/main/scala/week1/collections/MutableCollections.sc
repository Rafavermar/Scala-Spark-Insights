import week1.collections.MutableCollections._

// Adding and removing elements in the mutable buffer
numbersBuffer += 6
println(numbersBuffer)

// Modifying the set of numbers
numbersSet += 6
numbersSet -= 1
println(numbersSet)

// Updating the map with a new value for Italy
println("capitalsMap(\"Italy\"): " + capitalsMap("Italy"))

